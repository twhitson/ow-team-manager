<div ng-app="TeamModule" ng-controller="TeamController" ng-init="loadTeam(<%= id %>)" ng-cloak>
    
    <div class="row">
        <div class="col-md-6">
            <h1>{{team.name}}</h1>
        </div>
        <div class="col-md-6 text-right">
            <a href="javascript:void(0);" ng-click="deleteTeam(<%= id %>)" class="btn btn-danger"><i class="fa fa-trash"></i> Delete</a>
        </div>
    </div>
    
    <h2>Members</h2>
    
    <div class="panel panel-default">
        <table class="table table-striped table-hover">
            <thead>
                <tr>
                    <th width="15%">Battletag</th>
                    <th width="15%">Rank</th>
                    <th colspan="3">Most Played</th>
                    <th width="15%">Average K/D</th>
                    <th width="5%">&nbsp;</th>
                </tr>
            </thead>
            <tbody>
                <tr ng-repeat="member in team.teammembers | orderBy:'-profile.competitive.rank'">
                    <td>{{member.battletag}}</td>
                    <td>
                        <img src="{{member.profile.competitive.rank_img}}" ng-show="member.profile != null" height="24" width="24">
                        {{member.profile.competitive.rank}}
                        <span class="fa fa-spinner fa-spin" ng-show="member.profile == null"></span>
                    </td>
                    <td>
                        <div ng-repeat="hero in member.heroes | orderBy:percentage | limitTo:1">
                            <img src="{{hero.image}}" ng-show="member.heroes != null" class="img-circle" height="24" width="24">
                            {{hero.name}}
                        </div>
                        <span class="fa fa-spinner fa-spin" ng-show="member.heroes == null"></span>
                    </td>
                    <td>
                        <div ng-repeat="hero in member.heroes | orderBy:percentage | limitTo:2" ng-if="$index > 0">
                            <img src="{{hero.image}}" ng-show="member.heroes != null" class="img-circle" height="24" width="24">
                            {{hero.name}}
                        </div>
                        <span class="fa fa-spinner fa-spin" ng-show="member.heroes == null"></span>
                    </td>
                    <td>
                        <div ng-repeat="hero in member.heroes | orderBy:percentage | limitTo:3" ng-if="$index > 1">
                            <img src="{{hero.image}}" ng-show="member.heroes != null" class="img-circle" height="24" width="24">
                            {{hero.name}}
                        </div>
                        <span class="fa fa-spinner fa-spin" ng-show="member.heroes == null"></span>
                    </td>
                    <td>
                        {{(parseFloat(member.allheroes['Eliminations-Average']) / parseFloat(member.allheroes['Deaths-Average']))| number:2}}
                        <span class="fa fa-spinner fa-spin" ng-show="member.allheroes == null"></span>
                    </td>
                    <td>
                        <a href="javascript:void(0);" ng-click="deleteTeamMember(member.id)"><i class="fa fa-trash"></i></a>
                    </td>
                </tr>
            </tbody>
            <tbody>
        </table>
        <div class="panel-footer">
            <form class="form-inline text-center" ng-submit="submitAddMemberForm()" name="addmember">
                <div class="form-group" ng-class="{'has-error':addmember.battletag.$invalid && addmember.battletag.$dirty}">
                    <input type="text" class="form-control" name="battletag" ng-model="addmemberForm.battletag" ng-maxlength="50" required placeholder="Battletag">
                </div>
                
                <div class="form-group" ng-class="{'has-error':addmember.region.$invalid && addmember.region.$dirty}">
                    <select class="form-control" name="region" ng-model="addmemberForm.region" required>
                        <option value="eu" ng-selected="selected">EU</option>
                        <option value="us">US</option>
                        <option value="kr">KR</option>
                        <option value="cn">CN</option>
                        <option value="global">Global</option>
                    </select>
                </div>
                
                <div class="form-group">
                    <button type="submit" class="btn btn-primary" ng-disabled="addmember.$invalid">
                        <span ng-show="!addmemberForm.loading">Add</span>
                        <span class="fa fa-spinner" ng-show="addmemberForm.loading"></span>
                        <span ng-show="addmemberForm.loading">Processing...</span>
                    </button>
                </div>
            </form>
        </div>
    </div>
    
    <h2>Averages</h2>
    <div class="panel panel-default">
        <div class="panel-body">
            Stuff
        </div>
    </div>
    
</div>