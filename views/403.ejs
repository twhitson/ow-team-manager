<div class="text-center">
  
    <h1>Forbidden</h1>
    <h2>
        <% if (typeof data !== 'undefined') { %>
            <%= data %>
        <% } else { %>
            You don't have permission to see the page you're trying to reach.
        <% } %>
    </h2>
  
    <p><a href="http://en.wikipedia.org/wiki/HTTP_403">Why might this be happening?</a></p>
</div>